/*
 * Task enum contains the possible tasks available on a time card
 */
public enum Task {
    SmokePrevention,
    TheftPrevention,
    PublicContact,
    SafeWalks,
    Hazard_ServiceRequests,
    AssistSecurity
}
